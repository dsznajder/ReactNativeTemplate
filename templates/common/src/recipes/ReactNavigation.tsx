import React from 'react'
import Typography from '~/components/Typography'

export const ReactNavigation = () => {
  return (
    <Typography>
      This is navigation and it will appear if you select `react-navigation` module
    </Typography>
  )
}
